<Window x:Class="Diffusion.Toolkit.Windows.DuplicateDetectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        xmlns:converters="clr-namespace:Diffusion.Toolkit.Converters"
        mc:Ignorable="d"
        Title="Duplicate Detection" Height="700" Width="1000"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <converters:IECFormatConverter x:Key="IECFormatConverter"/>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Find Duplicate &amp; Variant Images" FontSize="18" FontWeight="Bold" 
                       Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBlock Text="Identify ORIG/FINAL pairs, workflow variants, and visually similar images" 
                       FontSize="12" Foreground="{DynamicResource TextBrush}" Opacity="0.7" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Detection Options -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Detection Mode:" Width="120" VerticalAlignment="Center"/>
                    <ComboBox x:Name="DetectionModeComboBox" Width="250" SelectedIndex="0" 
                              SelectionChanged="DetectionModeComboBox_SelectionChanged">
                        <ComboBoxItem Content="Filename Pattern (_ORIG/_FINAL)" Tag="filename"/>
                        <ComboBoxItem Content="Workflow Variants (Same Seed/Model/Prompt)" Tag="workflow"/>
                        <ComboBoxItem Content="Visual Similarity (CLIP Embeddings)" Tag="visual"/>
                    </ComboBox>
                </StackPanel>

                <!-- Visual similarity threshold slider (only visible for visual mode) -->
                <StackPanel x:Name="SimilarityPanel" Orientation="Horizontal" Visibility="Collapsed" Margin="0,0,0,10">
                    <Label Content="Similarity Threshold:" Width="120" VerticalAlignment="Center"/>
                    <Slider x:Name="SimilaritySlider" Width="150" Minimum="0.85" Maximum="0.99" Value="0.95" 
                            TickFrequency="0.01" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ElementName=SimilaritySlider, Path=Value, StringFormat={}{0:P0}}" 
                               Width="50" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <Button Grid.Column="1" x:Name="ScanButton" Content="Scan for Duplicates" 
                    Width="150" Height="35" Click="ScanButton_Click"
                    IsEnabled="{Binding CanScan}"/>
        </Grid>

        <!-- Results Panel -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Results List -->
            <Border Grid.Column="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource SecondaryBrush}" Padding="10,8">
                        <TextBlock Text="Detected Pairs/Groups" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                    </Border>

                    <ListView Grid.Row="1" x:Name="PairsListView" 
                              ItemsSource="{Binding Pairs}"
                              SelectionChanged="PairsListView_SelectionChanged"
                              Background="Transparent"
                              BorderThickness="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" 
                                                   TextTrimming="CharacterEllipsis" Foreground="{DynamicResource ForegroundBrush}"/>
                                        <TextBlock Foreground="{DynamicResource TextBrush}" Opacity="0.7" FontSize="11">
                                            <Run Text="{Binding OriginalResolution, Mode=OneWay}"/>
                                            <Run Text=" → "/>
                                            <Run Text="{Binding FinalResolution, Mode=OneWay}"/>
                                            <Run Text=" ("/>
                                            <Run Text="{Binding UpscaleFactor, StringFormat={}{0:F1}x, Mode=OneWay}"/>
                                            <Run Text=")"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding SavingsText}" FontSize="11" 
                                                   Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <!-- Empty state -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                                Visibility="{Binding HasNoPairs, Converter={StaticResource BoolToVisibilityConverter}}">
                        <fa:ImageAwesome Icon="Search" Width="48" Height="48" 
                                         Foreground="{DynamicResource TextBrush}" Opacity="0.3"/>
                        <TextBlock Text="No duplicates found" FontSize="14" Margin="0,15,0,0"
                                   Foreground="{DynamicResource TextBrush}" Opacity="0.5" TextAlignment="Center"/>
                        <TextBlock Text="Click 'Scan for Duplicates' to begin" FontSize="12" Margin="0,5,0,0"
                                   Foreground="{DynamicResource TextBrush}" Opacity="0.4" TextAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Preview Panel -->
            <Border Grid.Column="2" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource SecondaryBrush}" Padding="10,8">
                        <TextBlock Text="Comparison Preview" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}"/>
                    </Border>

                    <!-- Original Image -->
                    <Grid Grid.Row="1" Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,5,5,5">
                            <Border Background="#FF5555" CornerRadius="3" Padding="8,3" Margin="0,0,10,0">
                                <TextBlock Text="ORIGINAL" FontWeight="Bold" FontSize="10" Foreground="White"/>
                            </Border>
                            <TextBlock x:Name="OriginalInfoText" Text="" FontSize="11" VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextBrush}"/>
                        </StackPanel>
                        
                        <Border Grid.Row="1" Background="{DynamicResource TertiaryBrush}" CornerRadius="4" Margin="5">
                            <Image x:Name="OriginalImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                    </Grid>

                    <!-- Final Image -->
                    <Grid Grid.Row="2" Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,5,5,5">
                            <Border Background="#55AA55" CornerRadius="3" Padding="8,3" Margin="0,0,10,0">
                                <TextBlock Text="FINAL" FontWeight="Bold" FontSize="10" Foreground="White"/>
                            </Border>
                            <TextBlock x:Name="FinalInfoText" Text="" FontSize="11" VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextBrush}"/>
                        </StackPanel>
                        
                        <Border Grid.Row="1" Background="{DynamicResource TertiaryBrush}" CornerRadius="4" Margin="5">
                            <Image x:Name="FinalImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Statistics Bar -->
        <Border Grid.Row="3" Background="{DynamicResource SecondaryBrush}" CornerRadius="4" Padding="15,10" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Pairs Found" FontSize="11" Foreground="{DynamicResource TextBrush}" Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalPairs}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Selected" FontSize="11" Foreground="{DynamicResource TextBrush}" Opacity="0.7"/>
                    <TextBlock Text="{Binding SelectedCount}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Potential Savings" FontSize="11" Foreground="{DynamicResource TextBrush}" Opacity="0.7"/>
                    <TextBlock Text="{Binding TotalSavings}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Progress" FontSize="11" Foreground="{DynamicResource TextBrush}" Opacity="0.7"/>
                    <TextBlock Text="{Binding ProgressText}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource ForegroundBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SelectAllButton" Content="Select All" Width="100" Height="32" Margin="0,0,10,0"
                    Click="SelectAllButton_Click" IsEnabled="{Binding HasPairs}"/>
            <Button x:Name="SelectNoneButton" Content="Select None" Width="100" Height="32" Margin="0,0,10,0"
                    Click="SelectNoneButton_Click" IsEnabled="{Binding HasPairs}"/>
            <Button x:Name="MarkForDeletionButton" Content="Mark Originals for Deletion" Width="180" Height="32" Margin="0,0,10,0"
                    Click="MarkForDeletionButton_Click" IsEnabled="{Binding HasSelectedPairs}"/>
            <Button x:Name="CloseButton" Content="Close" Width="100" Height="32" 
                    Click="CloseButton_Click" IsCancel="True"/>
        </StackPanel>

        <!-- Processing Overlay -->
        <Border Grid.RowSpan="5" Background="#AA000000" CornerRadius="4"
                Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <fa:ImageAwesome Icon="CircleOutlineNotch" Width="48" Height="48" Spin="True" SpinDuration="1"
                                 Foreground="{DynamicResource ForegroundBrush}"/>
                <TextBlock Text="{Binding ProcessingMessage}" FontSize="14" Margin="0,15,0,0"
                           Foreground="{DynamicResource ForegroundBrush}" TextAlignment="Center"/>
                <ProgressBar Width="300" Height="8" Margin="0,10,0,0" IsIndeterminate="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
